/** Browser stub — vocabulary loader uses import.meta.glob in Vite. */
export function dirname(): string {
  return "";
}

export function join(...parts: string[]): string {
  return parts.join("/");
}
