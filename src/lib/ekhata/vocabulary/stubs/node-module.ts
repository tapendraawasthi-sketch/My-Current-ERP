/** Browser stub — vocabulary loader uses import.meta.glob in Vite; createRequire is never called. */
export function createRequire(): never {
  throw new Error("node:module is not available in the browser");
}
