# UI-7 — Voucher Grid Spec

Foundation: `JournalVoucherForm` debit/credit lines.

Debit and credit visually distinct with text labels (not colour alone). Running debit/credit totals visible. Authoritative balance check remains in `postJournalTransaction`. Restricted accounts follow domain. Mobile: prefer desktop for complex journals; row editor when constrained.
