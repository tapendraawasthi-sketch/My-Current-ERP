# UI-7 — Transaction Keyboard and Productivity Spec

- Logical Tab / Shift+Tab
- Enter: submit field / next cell per existing form convention — never one-keystroke post
- Escape: close active overlay only
- Ctrl/Cmd+S: save draft where form already supports it
- Focus restore after Dialog/Drawer/row delete
- No keyboard trap; visible focus; reduced motion respected
