services:
  - type: web
    name: sutra-erp
    runtime: node
    buildCommand: npm run build
    startCommand: node serve.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
